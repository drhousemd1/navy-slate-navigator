
// This is a re-export from the standardized location to maintain backward compatibility
// with existing code that imports from the ui folder
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
